{
  "name": "allowance-tracker",
  "version": "1.0.0",
  "description": "Simple allowance tracking app with a Postgres-backed server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
